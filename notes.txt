============this file is just to type the most importent notes from the course.============
the general syntax for the styling is:
selector{
    property : value;
    property : value;
}

selector may be -->id
                -->class

in case it is class the syntax will be like that
.elemnt-name{
    property: value;
}
in case it is id the syntax will be like that
#class-name{
    property :value;
    
}

border : 
`->it has three main property 
    -size  - color -style